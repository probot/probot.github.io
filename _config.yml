title: Probot
description:
permalink: pretty

exclude:
  - Gemfile*
  - node_modules
  - package.json
  - package-lock.json
  - README.md
  - node_modules

sass:
  style: :compressed
  sass_dir: _sass
  load_paths:
    - node_modules
    - node_modules/**/*/node_modules

collections:
  plugins:
    output: true
    permalink: /apps/:name/

defaults:
  - scope:
      path: ""
      type: plugins
    values:
      layout: plugin
  - scope:
      path: "docs"
    values:
      layout: docs

gems:
  - jekyll-octicons
